package cathy.topicdiscovery;

public class CATHY_runtime {

	public static void main(){
//	%% tic starts
//	start = tic; 
//	uumat = triu(pumat'*pumat); %unigram-unigram network
//
//	clear root
//	clear options
//	root.edge = cell(1);
//	[i,j,s] = find(uumat);
//	root.edge{1}=[i,j,s];
//	clear uumat
//	root.depth = 0;
//	% root.selk = 'dblp';  
//	% the way of selecting number of topics; dblp means for top level 
//	% k=root.range and for other levels k=4
//	root.range = 5;
//	root.k = root.range;
//	options.maxdepth = 2;   % the maximal depth of the tree; root's depth is 0
//	options.EMiter = 100;    % the number of iterations of the EM steps
//	options.EPSI = 1e-6;  % the convergence threshold
//	options.Iter = 1;   % the number of random restart
//
//	root.map = 1:length(udict);
//
//	root.prefix = [folder '/cathy4tensor']; % the output path
//	tree = BuildTree(root, options);   % the recursive construction
//	rt = toc(start)

	}
	
}
